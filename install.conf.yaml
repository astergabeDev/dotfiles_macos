- defaults:   
    link:
      relink: true

- clean: ['~']

- link:
#   ~/.emacs.d:
#   ~/.emacs: 
    ~/.bash:
    ~/.bashrc:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/kitty/kitty.conf: kitty/kitty.conf
- create:
    
- shell:
#   - [git submodule update --init --recursive, Installing submodules] 
